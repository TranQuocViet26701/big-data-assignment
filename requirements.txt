# Python dependencies for Inverted Index MapReduce Project
#
# ============================================================================
# IMPORTANT: MASTER NODE ONLY!
# ============================================================================
# These dependencies are ONLY needed on the MASTER NODE for the download
# script (donwload_file.py). Worker nodes need NO special dependencies!
#
# The MapReduce mapper uses ONLY built-in Python modules (sys, re, os).
# This simplifies deployment and eliminates cross-node dependency issues.
# ============================================================================
#
# INSTALLATION INSTRUCTIONS:
#
# === For Python 3.11 and earlier: ===
# pip3 install --user -r requirements.txt
#
# === For Python 3.12+ (Ubuntu 24.04+, Debian 13+): ===
# pip3 install --user --break-system-packages -r requirements.txt
#
# -----------------------------------------------------------------------------

# Data Processing (for download script)
pandas>=1.3.0

# Web Scraping (for download script)
requests>=2.28.0
beautifulsoup4>=4.11.0
gutenberg>=0.8.0

# Terminal Output Formatting (for colored output)
colorama>=0.4.6
